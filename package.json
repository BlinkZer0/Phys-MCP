{
  "name": "phys-mcp",
  "version": "0.1.0",
  "description": "Physics MCP Server with CAS, Plot, and NLI capabilities",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "node scripts/build.js",
    "build:workspace": "npm run build --workspaces",
    "build:pnpm": "pnpm -r build",
    "dev": "scripts\\dev.bat",
    "dev:all": "node scripts/dev-all.js",
    "format": "bash scripts/format.sh",
    "lint": "eslint \"packages/*/src/**/*.ts\"",
    "lint:fix": "eslint \"packages/*/src/**/*.ts\" --fix",
    "typecheck": "pnpm -r typecheck",
    "clean": "npm run clean --workspaces && rm -rf node_modules",
    "clean:pnpm": "pnpm -r clean && rm -rf node_modules",
    "test": "npm run test --workspaces",
    "test:install": "node scripts/test-installation.js",
    "test:coverage": "pnpm -r test:coverage",
    "install:python": "cd packages/python-worker && pip install -r requirements.txt",
    "healthcheck": "node scripts/healthcheck.js",
    "precommit": "pnpm lint && pnpm typecheck",
    "prepare": "husky install",
    "gen:manifest": "tsx scripts/generate-tools-manifest.ts",
    "svg:optimize": "node scripts/svg-optimize.mjs",
    "svg:build": "node ./scripts/svg/generate-headers.mjs && node ./scripts/svg/export-pngs.mjs && svgo -f assets/svg/headers -o assets/svg/headers --config=./scripts/svg/svgo.config.mjs",
    "svg:check": "svgo -f assets/svg/headers --config=./scripts/svg/svgo.config.mjs --multipass --quiet",
    "svg:all": "npm run svg:build && npm run svg:check",
    "docs:links": "node scripts/docs-link-check.mjs",
    "docs:mermaid:smoke": "node scripts/docs-mermaid-smoke.mjs",
    "docs:generate": "node scripts/generate-docs.js"
  },
  "devDependencies": {
    "@mermaid-js/mermaid-cli": "^10.6.1",
    "@types/node": "^20.19.17",
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "@typescript-eslint/parser": "^6.0.0",
    "@vitest/coverage-v8": "^1.0.0",
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "eslint": "^8.0.0",
    "eslint-config-prettier": "^9.0.0",
    "eslint-plugin-prettier": "^5.0.0",
    "fast-glob": "^3.3.3",
    "husky": "^8.0.0",
    "prettier": "^3.0.0",
    "sharp": "^0.33.4",
    "svgo": "^3.3.2",
    "tsx": "^4.20.6",
    "typescript": "^5.9.2",
    "vitest": "^1.0.0",
    "zod-to-json-schema": "^3.24.6"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@8.15.0",
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.18.2",
    "dotenv": "^16.3.0",
    "zod": "^3.22.0"
  }
}
